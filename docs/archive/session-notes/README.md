# Session Notes Archive

This folder stores old root-level handoff and repository-state notes.

These files are useful for provenance but should not be default AI context. Use `docs/ai-context-summary.md`, `docs/context-index.md`, and `docs/PHASE_REGISTRY.md` first.
